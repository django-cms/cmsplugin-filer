[tox]
envlist=pytest

[testenv:pytest]
install_command=
    pip install --pre --use-mirrors -b .tox/build-pytest {opts} {packages}
deps=
    pytest-django
changedir=
    {envdir}
commands=
    py.test \
    	--ds=cmsplugin_filer_image.tests.settings \
    	--junitxml={toxinidir}/pytest-results.xml \
		--pyargs \
		{posargs:cmsplugin_filer_image.tests.tests}
